{
  "routes": [{ "handle": "filesystem" }, { "src": "/.*", "dest": "index.html" }],
  "build": {
    "env": {
      "CI": "false"
    },
    "command": "npm install --legacy-peer-deps && npm run build"
  }
}
